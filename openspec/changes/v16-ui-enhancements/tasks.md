## 1. Period Chip Spacing

- [x] 1.1 Removed `Modifier.weight(1f)` from each FilterChip.
- [x] 1.2 Added `Modifier.padding(horizontal = 6.dp)` to each FilterChip.

## 2. Tag Reorder Button Fix

- [x] 2.1 Removed `.clickable { onRenameClick(tag) }` from parent `Row`.
- [x] 2.2 Added `.clickable { onRenameClick(tag) }` to the tag name `Text`.

## 3. About Page Version

- [x] 3.1 `"v1.5"` → `"v1.6"`.
